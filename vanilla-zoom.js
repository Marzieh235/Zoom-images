(function (window) {
    let defineLibrary = () => ({
        init : 
    })

    if (typeof (vanillaZoom) == 'undefind') {
        window.vanillaZoom = defineLibrary();
    } else {
        console.log('Library already defined.')
    }

})(window)